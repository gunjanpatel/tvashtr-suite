// Nuxt Layer configuration for @tvashtr/ui
// All components, composables, and plugins in app/ are auto-imported
// into any Nuxt app that extends this layer.
export default defineNuxtConfig({
  future: {
    compatibilityVersion: 4,
  },
})
